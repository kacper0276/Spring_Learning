spring:
  application:
    name: todo-app
  data:
    rest:
      base-path: false
  datasource:
    url: jdbc:mysql://localhost:3306/spring
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update